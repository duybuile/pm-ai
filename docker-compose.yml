services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: pm-ai:latest
    container_name: pm-ai
    ports:
      - "8501:8501"
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
